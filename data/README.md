# Data Directory

Input data files are intentionally not tracked by Git.

Keep the project data under:

```text
data/co2/
data/delta13c/
data/delta14c/
```

Only the directory structure and README files are tracked. Place the required observational files in the corresponding subdirectory before running the fitting, residual-analysis or paper-figure scripts.
