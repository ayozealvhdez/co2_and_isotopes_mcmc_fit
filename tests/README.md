# Tests

This directory contains internal tests, but it is shown for transparency.

Test are designed to check that the functions from the project have the expected behaviour and that they fulfill the logic described in the paper.

You can run the full test suite like this:

```bash
python -m tests.run_tests
```

